image: "python:3.7"

before_script:
    - pip install -r dev_requirements.txt
    
stages:
    - pages 

pages:
    stage: pages
    script: 
    - sphinx-build -b html docs/source public
    artifacts:
        paths:
            - public
    only:
        - master
